package com.assignment.exc.util;

import java.util.Comparator;

import com.assignment.exc.model.Share;

public class ShareComparator implements Comparator<Share>{

	@Override
	public int compare(Share o1, Share o2) {
		// TODO Auto-generated method stub
		//if(o1.getHigh() > (o2.getLow()));
		//if(o1.getClosedHours().compareTo(o2.getClosedHours()))
		return 1;
	}
}
